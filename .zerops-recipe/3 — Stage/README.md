# Deck Renderer — Stage Environment

This is a stage environment for [Deck Renderer (info + deploy)](https://app.zerops.io/recipes/deck-renderer?environment=stage) recipe on [Zerops](https://zerops.io).

<!-- #ZEROPS_EXTRACT_START:intro# -->
**Stage** environment runs the production `zerops.yaml` setups (static SPA,
API, Chromium worker) on a single container each, with hobby PostgreSQL,
Valkey, and NATS — cheaper than Small Production.
<!-- #ZEROPS_EXTRACT_END:intro# -->
