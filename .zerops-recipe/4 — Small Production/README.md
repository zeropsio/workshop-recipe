# Deck Renderer — Small Production Environment

This is a small production environment for [Deck Renderer (info + deploy)](https://app.zerops.io/recipes/deck-renderer?environment=small-production) recipe on [Zerops](https://zerops.io).

<!-- #ZEROPS_EXTRACT_START:intro# -->
**Small production** is the entry production tier: one container per
service, `oltp-staging` PostgreSQL, staging Valkey, NATS, and the
production SPA / API / Chromium worker setups.
<!-- #ZEROPS_EXTRACT_END:intro# -->
