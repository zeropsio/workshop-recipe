# Deck Renderer — AI Agent Environment

<!-- #ZEROPS_EXTRACT_START:intro# -->
**AI agent** environment gives a coding agent a `zcp` workspace plus
`*dev` workspaces and `*stage` deploy targets for the SPA, API, and Chromium
worker, with hobby PostgreSQL, Valkey, and NATS on the same private network.
Import [`import.yaml`](./import.yaml) from this folder.
<!-- #ZEROPS_EXTRACT_END:intro# -->
